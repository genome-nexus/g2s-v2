# Copy to application-local.properties (gitignored), then run with:
#   --spring.profiles.active=local   (after -jar; avoid -D on PowerShell)
# start-web.ps1 / start-services.ps1 already enable profile local.

# Use rebuilt pipeline database (default prod name is pdb):
spring.datasource.url=jdbc:mysql://localhost:3306/pdb_2026?useSSL=false

# Windows example (adjust drive/path):
workspace=C:/CursorProjects/cbioportal260530/g2s/workdir/
uploaddir=C:/CursorProjects/cbioportal260530/g2s/tmp/upload
